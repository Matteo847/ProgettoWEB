<!DOCTYPE html>
<html lang="en">

<head>
	<title>Build Genshin</title>
	<link rel="icon" href="images/icona.webp" />
	<meta charset="utf-8">
	<link rel="stylesheet" href="css/gilda.css">
	<link rel="stylesheet" href="css/style.css">
	<link rel="stylesheet" href="css/mostraGilda.css">
	<link rel="stylesheet" href="css/build.css">
	<link rel="stylesheet" href="css/mostraGilda.css">
	<link rel="stylesheet" href="css/footer.css">

	<link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>

<body class="colore-sfondo">
	<%- include('navbar-noRIc.ejs') %>

	<% if (typeof error !== 'undefined' && error.length > 0) { %>
	<div class="alert alert-danger mt-3">
		<%= error %>
	</div>
	<% } %>

	<div class="container my-5">

		<!-- Pulsante indietro in alto -->
		<div class="mb-3">
			<a class="btn btn-secondary" href="/gilda">
				⬅ Torna indietro
			</a>
		</div>

		<div class="container my-5">
			<div class="d-flex justify-content-between align-items-center mb-3">
				<div class="flex-grow-1 text-center">
					<h1 class="mb-2"><%= gilda.nome_gilda %></h1>
					<p class="fs-4"><%= gilda.descrizione_gilda %></p>
				</div>

				<div class="ms-3">
					<% if (membri >= gilda.limite_partecipanti) { %>
					  	<span class="text-danger">La gilda ha raggiunto il limite massimo</span>
					<% } else { %>
						<% if (isLeader) { %>
							<form action="/gilda/sciogli" method="POST" onsubmit="return confirm('Sei sicuro di voler sciogliere la gilda?');">
								<input type="hidden" name="id_gilda" value="<%= gilda.id %>">
								<button type="submit" class="btn btn-success">Sciogli gilda</button>
							</form>
						<% } else { %>
							<% if (isMembro) { %>
								<form action="/gilda/esci" method="POST" style="display:inline;">
									<input type="hidden" name="gildaId" value="<%= gilda.id %>">
									<button type="submit" class="btn btn-danger">Esci dalla gilda</button>
								</form>
							<% } else { %>
								<form action="/gilda/unisciti" method="POST" style="display:inline;">
									<input type="hidden" name="gildaId" value="<%= gilda.id %>">
									<button type="submit" class="btn btn-success">Unisciti alla gilda</button>
								</form>
							<% } %>
						<% } %>
					<% } %>
				</div>
			</div>

			<p><strong>Lingua:</strong> <%= gilda.lingua %></p>
			<p><strong>Limite partecipanti:</strong> <%= nmembri %>/<%= gilda.limite_partecipanti %></p>

			<hr>

			<!-- Elenco utenti fittizi -->
			<h3 class="mt-4">👥 Membri della gilda</h3>
			<ul>
				<% membri.forEach((m)=> { %>
					<li class="d-flex justify-content-between align-items-center">
						<%= m.username %> (<%= m.ruolo %>)
						<% if (isLeader && m.ruolo !== 'leader') { %>
							<form action="/gilda/rimuovimembro" method="POST" onsubmit="return confirm('Sei sicuro di voler rimuovere questo membro?');" style="display:inline;">
								<input type="hidden" name="id_gilda" value="<%= gilda.id %>">
								<input type="hidden" name="id_utente" value="<%= m.id_utente %>">
								<button type="submit" class="btn btn-danger btn-sm">Rimuovi</button>
							</form>
						<% } %>
					</li>
				<% }); %>
			</ul>
			<hr>
			<h3 class="mt-4">🛡️ Build dei membri</h3>
				<%if (build.length === 0) { %>
					<div class="nessuna-build">
						<strong>NESSUNA BUILD DISPONIBILE</strong>
					</div>
				<% } else { %>
					<ul>
						<% build.forEach((b)=> { %>
							<li class="d-flex justify-content-between align-items-center">
								<div>
									<%- include('partials/build_card', { b: b, mipiace: [] }) %>
							</div>
							<% if (isLeader) { %>
								<form action="/gilda/rimuoviBuild" method="POST" onsubmit="return confirm('Sei sicuro di voler rimuovere questo build?');" style="display:inline;">
									<input type="hidden" name="id_gilda" value="<%= gilda.id %>">
									<input type="hidden" name="id_build" value="<%= b.id_build %>">
									<button type="submit" class="btn btn-danger btn-sm">Rimuovi</button>
								</form>
							<% }; %>
						</li>
					<% }); %>
				</ul>
				<% } %>
		</div>
	</div>
	<footer class="footer navbar-sfondo">
		<div class="container">
			<div class="row">
				<div class="col-md-3 align-items-center">
					<img src="images/icona.webp" class="footer-img">
				</div>
				<div class="col-md-3 align-items-center">
					<p class="footer-text">Build Genshin</p>
				</div>
				<div class="col-md-6 justify-content-end align-items-center">
					<p class="email-footer">Seguimi su instagram:</p>
					<p>matteo_dura</p>
					<p class="email-footer">Email:</p>
					<p>20050673@studenti.uniupo.it</p>
					<p class="email-footer">© 2024 Build Genshin. Tutti i diritti riservati.</p>
				</div>
			</div>
		</div>
	</footer>
</body>
</html>
